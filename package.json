{
  "name": "pacman-battle-standalone",
  "version": "1.0.0",
  "description": "Standalone offline version of Pacman Battle for APK/Web",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist/js && cp index.html dist/ && cp js/* dist/js/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}