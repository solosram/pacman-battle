{
  "name": "pacman-battle-standalone",
  "version": "1.0.0",
  "description": "Standalone offline version of Pacman Battle for APK/Web",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed for raw HTML/JS'",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {},
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}